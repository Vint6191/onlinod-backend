"use strict";

const CREATOR_WRITE_LANE_STATUSES = Object.freeze([
  "CLAIMED",
  "RUNNING",
  "COMMITTING",
  "RECONCILE_REQUIRED",
]);

const PRECOMMIT_MUTABLE_STATUSES = Object.freeze([
  "QUEUED",
  "CLAIMED",
  "RUNNING",
  "RETRY_SCHEDULED",
  "PAUSED",
]);

const ACTIVE_WRITE_WORKFLOW_STATUSES = Object.freeze([
  "QUEUED",
  "CLAIMED",
  "RUNNING",
  "COMMITTING",
  "RECONCILE_REQUIRED",
  "RETRY_SCHEDULED",
  "PAUSED",
]);

module.exports = {
  CREATOR_WRITE_LANE_STATUSES,
  PRECOMMIT_MUTABLE_STATUSES,
  ACTIVE_WRITE_WORKFLOW_STATUSES,
};
